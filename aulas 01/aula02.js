// AND (&&)
let idade = 18
let vistoVerificado = true
let resutado = (idade >= 18) && (vistoVerificado === true)
console.log(resutado)

/////////////////////////////////////////////////////////////////
// AND (&&)

let moedasColetadas = 100
let itensestrelas = "estrela"
let resultado = (moedasColetadas === 100) && (itensestrelas ==="estrela")
console.log(resultado)


////////////////////////////////////////////////////////////////////////

// OR (||)

let tempo = "chuva"
let item = "guarda chuva"
let podeSair = (tempo !== "chuva") || (item === "guarda chuva")

console.log("o Nosso Personagem pode Sair ? " +  podeSair)



//////////////////////////////////////////////////////////////////
// NOT ( ! ) - negar uma afirmação

let tempoTempo = "chuva"
let horario = 8
let ressultado = !((tempoTempo !== "chuva") && (hprario > 6))
console.log(ressultado)